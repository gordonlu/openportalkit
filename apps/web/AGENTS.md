# Web App Instructions

This app uses Next.js App Router.

Before changing framework-specific APIs, check the installed Next.js docs under `node_modules/next/dist/docs/` because this project currently uses Next.js 16.

Public pages must preserve:

- server-rendered or statically rendered content
- semantic HTML
- accessible links and forms
- stable URLs
- AgentSEO outputs when public resources are added
