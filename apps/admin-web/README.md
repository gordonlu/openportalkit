# Admin Web

The initial admin host is `src/OpenPortalKit.AdminHost` using ASP.NET Core Razor Pages.

This directory is reserved for a future separate admin frontend only if the server-first admin approach becomes insufficient.
