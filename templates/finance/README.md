# Finance Project Profile

Use `opk new --profile finance` for the disclosure-centre reference experience. The generated project records the
optional Finance Pack selection; finance content types, datasets, rules, templates, and dashboard cards remain only
under `industry-packs/Finance` and are never silently installed.
