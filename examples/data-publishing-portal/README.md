# Data Publishing Portal Example

Run the shared Web app and open `http://localhost:3000/examples/data`.

This example is catalogue-first rather than article-first. It emphasizes schema compliance, freshness, export,
and provenance while retaining searchable explanatory content.
