# Activity Portal Example

Run the shared Web app and open `http://localhost:3000/examples/activity`.

This example uses a poster-like event lead and agenda grid with registration state, accessible guidance, reports,
and recordings. It remains a publishing experience rather than a booking or ticketing system.
