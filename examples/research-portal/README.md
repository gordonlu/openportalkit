# Research Portal Example

Run the shared Web app and open `http://localhost:3000/examples/research`.

This example uses a journal-style image plate and reading grid for findings, methods, supporting datasets, and
briefings. The research vocabulary remains example content and does not extend the core domain model.
